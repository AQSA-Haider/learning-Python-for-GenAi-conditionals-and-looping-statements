Here i have uploaded some of the tasks that I have performed as I have started my python journey for GenAi . the topics that aur covered are
## Task 2.1: Discount Rules (if/elif/else):
Created a program that reads an integer order_amount using input() :
where the user will enter the amount of there order.
## Checking different conditions using if/elif/else:
#if order_amount>=2000:
 the discount will be 15%.
#if 1500<= order_amount<2000:
 the discount will be 10%.
#if 1000<= order_amount<1500:
 the discount will be 7%.
#if  order_amount<1000:
 the discount will be 0% thus,
## subtotal=order_amount-discount. 
#also, a fixed tax of 5% is applied on every order_amount
thus,
## final amount=subtotal + tax.
## Task 2.2: Process multiple orders(for loop):
Computing and printing the revenue.
## creating a list of list to print a table.
Table representing order_amount,dicount,and final_amount of the orders:
The revenue from these orders.
#Optional: total no. of orders that recieved a discount.   
## Task 2.3:User Menu(While loop + break/continue):
 Creating a user action menu using simple while loop:
   asking the user to choose, until they press 'q' to Quit.
   # Menu options:
   :1--Add order_amount to running list(using(input)).
   :2--Shows all orders and totals after applying discounts.
   :q--Quit. 
## Task2.4:Loop Control With Conditions:
## Applying the conditions with loop control statements :
#if a day sales is -1,it is treated as corrupted data and break the loop(stop processing).
#if a day sales is 0, it is treated as day with no sales and continue the loop(skip adding the revenue).
#valid sales has been added to total_sales nd the running total is being printed.

Author Name: Aqsa Haider
